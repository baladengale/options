# Do-Not-Wheel — MANUAL OVERRIDE only.
#
# The engine no longer auto-writes to this file. Hand-edit entries here to
# force-skip a ticker from the wheel (applies in screener + portfolio).
# Expired entries are ignored automatically.
#
# The watchlist is the master list. The daily eligibility check
# (is_wheel_eligible, moomoo-only) auto-skips loss-makers — no manual entry
# needed for unprofitable companies. Use this file only for a temporary,
# subjective force-skip you want to enforce regardless of profitability.
#
# Format:
#   TICKER:
#     added_date: 'YYYY-MM-DD'
#     expiration_date: 'YYYY-MM-DD'
#     months: 6
#     reason: why you're force-skipping it
{}
